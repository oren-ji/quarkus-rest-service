package ph.opensource.api.constant

/**
 * API error message
 *
 * Purpose: Define constantly repeating error message.
 *
 * @constructor Create empty API error message
 */
object APIErrorMessage {
    const val FAILURE_NOTICE = "Failure occurred."
    const val INVALID_REQUEST = "invalid request"
    const val TEMPORARILY_UNAVAILABLE = "service temporarily unavailable, we will be back shortly"
}